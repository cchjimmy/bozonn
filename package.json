{
	"name": "bozonn",
	"version": "1.0.3",
	"description": "simple neural network implementation in javascript",
	"scripts": {
		"start": "node main",
		"build": "rm -rf build/* && esbuild ./src/exports.js --bundle --format=esm --outfile=build/bozonn.js",
		"dev": "npm run build && npm run start"
	},
	"type": "module",
	"keywords": [
		"neural network"
	],
	"author": "cchjimmy",
	"license": "MIT",
	"devDependencies": {
		"esbuild": "^0.25.0"
	}
}
